import 'package:flutter/material.dart';

class Filials extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: new Scaffold(
          appBar: AppBar(
            backgroundColor: Color(0xFFE0253A),
          ),
      ),
    );
  }
}
